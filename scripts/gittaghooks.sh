#!/bin/bash
git config --global init.templatedir '~/.git_template'
echo "Hook templates installed!"
echo "To apply hooks to existing repositories, type 'git init' from the root folder of the git repo"
